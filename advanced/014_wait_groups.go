// 014 Wait Groups

package main

import (
	"fmt"
	"sync"
	"time"
)

// Step: 3)
// CONSTRUCTION EXAMPLE
type Worker struct {
	ID   int
	Task string
}

// PerformTask simulates a worker performing a task
func (w *Worker) PerformTask(wg *sync.WaitGroup) {
	defer wg.Done()
	fmt.Printf("WorkerID %d started %s\n", w.ID, w.Task)
	time.Sleep(time.Second)
	fmt.Printf("WorkerID %d finished %s\n", w.ID, w.Task)
}

func main() {
	var wg sync.WaitGroup

	// Define tasks to be performed by workers
	tasks := []string{"digging", "laying bricks", "painting"}

	for i, task := range tasks {
		worker := Worker{ID: i + 1, Task: task}
		wg.Add(1)
		go worker.PerformTask(&wg)
	}

	// Wait for all workers to finish
	wg.Wait()

	// Construction is finished
	fmt.Println("Contruction finished")
}

// Step: 2)
// // EXAMPLE WITH CHANNELS
// func worker(id int, tasks <-chan int, results chan<- int, wg *sync.WaitGroup) {
// 	defer wg.Done()
// 	fmt.Printf("WorkerID %d starting.\n", id)
// 	time.Sleep(time.Second) // simulate some work
// 	for task := range tasks {
// 		results <- task * 2
// 	}
// 	fmt.Printf("WorkerID %d finished\n", id)
// }

// func main() {
// 	var wg sync.WaitGroup
// 	numWorkers := 3
// 	numJobs := 5
// 	results := make(chan int, numJobs)
// 	tasks := make(chan int, numJobs)

// 	wg.Add(numWorkers)

// 	for i := range numWorkers {
// 		go worker(i+1, tasks, results, &wg)
// 	}

// 	for i := range numJobs {
// 		tasks <- i + 1
// 	}
// 	close(tasks)

// 	go func() {
// 		wg.Wait()
// 		close(results)
// 	}()

// 	for result := range results {
// 		fmt.Println("Result:", result)
// 	}
// }

// Step: 1)
// // ============ BASIC EXAMPLE WITHOUT USING CHANNELS
// func worker(id int, wg *sync.WaitGroup) {
// 	defer wg.Done()
// 	// wg.Add(1)  /// WRONG PRACTICE
// 	fmt.Printf("Worker %d starting\n", id)
// 	time.Sleep(time.Second) // simulate some time spent on processing the task
// 	fmt.Printf("Worker %d finished\n", id)
// }

// func main() {
// 	var wg sync.WaitGroup
// 	numWorkers := 3

// 	wg.Add(numWorkers) // THIS IS THE CORRECT WAY OF ADDING COUNTER TO WAIT GROUPS

// 	// Launch workers
// 	for i := range numWorkers {
// 		go worker(i, &wg)
// 	}

// 	wg.Wait() // blocking mechanism
// 	fmt.Println("All workers finished")
// }

/*
A weird group is a synchronization primitive provided by the sync package in go. It is used to wait for a collection of go routines to complete their execution. So what it means, essentially, is that we have another mechanism to wait for go routines to finish other than using channels. The reason we use wait groups is for synchronization. We wait for multiple go routines to finish before proceeding. Wait groups also help us in coordination. Wait groups coordinate the completion of concurrent tasks, ensuring that all tasks are completed before moving on. Wait groups also help us with resource management. Wait groups manage resources and clean up after concurrent operations. To create an instance of a wait group, we use Syncpackage and the actual function is async dot wait group. Now the instance of wait group offers us some basic operations. The most frequent operations that we will use in our day to day programming. So those operations are done using a few methods. So the first method, the function it is add and it takes an argument which is of type int. Add method increments the counter wait group counter by the argument that we pass it. Typically it is used to indicate the number of goroutines to wait for. Next we have the done method which is available on the instance of wait group. The done method decrements the counter by one. It doesn't take any arguments and by default it just decrements the counter by one. This method should be called inside each goroutine when it finishes its task. And finally we have the wait method. The wait method blocks until the counter inside the wait group is decremented to zero. Initially, we added the total count of all the goroutines. Then as each goroutine finishes its task, it will call the done method and from the total counter as Goroutines finish their task and they call the done method, a count of one will be decremented from the total counter, and until the total count becomes zero, the wait method will block the execution flow. And once the total count is zero, that means that we don't have to wait for any go routine. Now all go routines have finished their task, and then program execution moves to the next line after the wait method. So now let's see where groups in action in an example. So the first thing that we need to do is create a worker. Pass it an id int and then a wait group because we will be calling wait group dot done when the task is finished. So let's call the wait group. So wait group is of type sink dot wait group. Now let's define our function and it's not inside the main function. Sorry about that. It's outside perfect. So the first thing that we do is we defer the execution of wait group dot done WG dot done. We will call this and use the defer keyword before this so that once this function is about to return, that means once this function has performed all the tasks, then right before that it will call wg dot done and it will decrement the counter from the total counter of the wait group. Now let's define what it will be doing. So this function will be starting some work and then finishing it after some time. So we type worker percentage D and this should be f. So worker percentage d starting because we are just starting off right now and ID. Now let's simulate some time that is spent on performing this task. So time dot sleep and time dot second. Simulate some time spent on processing the task. And then finally we print a statement that a worker has finished the task. So we'll actually keep it even more simple. So worker one. And pass it ID and that's it. Now let's create a worker group inside our main function. So create a worker group with w g as the variable. And then it is of type sink dot wait group. Next we create the number of workers. So the number of workers are going to be three. And this program will be a very simple program. And first we'll try to understand this, and then we'll move on to a little more functionality in our weight group program. So Numworkers is three. So now let's add the worker's count to the weight group. And we do that by calling WG dot add. So as soon as I hit dot then we see that we have add done weight right. So we are going to do add and add not three but num workers. That's why we have a variable that holds the total number of workers. Now let's launch the workers. So launch workers for I in range NUM workers. We are starting off with the go routines now. So use go keyword with worker. So we are calling worker functions now with AI as the first argument and now the second argument that it needs. As we can see here, it needs a pointer to the sync dot wait group. So what it needs is the memory address so that it can update the wait group variable. Not a copy of wait group variable. All right. So what we do is we type ampersand and then w g. So that's the memory address so that the source is updated not a copy is updated because that will be destroyed once this for loop is complete. And now what we do is we wait for the go routines to finish. So w dot wait and that's it. No arguments needed here and last line will be fmt.println so that we know that if WC dot wait has a blocking mechanism, then our println statement will not be printed until all the wait groups have finished. So let's see that in action as well. All workers finished. Perfect. So let's see how it goes. So save this. Run this go run wait groups. And here we go again printf. So newline character and run it again. All right. So worker 201 started. And then 120 finished. So. The order of finishing could be different because it depends on the go runtime scheduler and our operating system are our processor threads, how they execute these go routines. Which one gets finished first? Which one gets assigned a processor thread first? So that is something that we don't know yet. So the output will be a little different each time. Let's run it again. And this time it's 210. So sometimes it's 120. Sometimes it's 021 and maybe 210. So it's a different sequence of finishing the go routines. Perfect. So this time we haven't used any channels. We are not waiting for the go routines to finish by using a channel. And that's why I haven't used the results variable here, because I wanted to create an example where we would not use any channel at all, and see how we can still wait for the go routines to finish, and then have our program end in a graceful manner. So wait groups help us accomplish that. All right, so let's go over how this is working. So the first thing that we did is we made a worker function. And this is going to be our go routine. So it receives an ID which is an integer type. And we assign this ID to the worker. Once that is done the second argument wait group we use that to execute wait group dot done. So done. Method. What this will do is it will decrement the wait group counter by one. And after that we print a statement that our worker is starting now. And in the next line it starts and it takes one second to perform a task. And in the next line we give a confirmation that the worker has finished in the main function. We now initialize the weight group. So. Weight group is initialized using the sink package. And then from this sink package we call the weight group. Type the weight group struct. And in the weight group struct we see that there are add done and weight methods available for us, and we have already used one of them. Weight group dot done here, so the remaining functions will be used in the subsequent lines. So in the next line we fix the number of workers that we are going to create. And after that we come to w dot add. So weight group dot add adds the number of workers to the total counter. All right. So here we pass the number three to W dot add. And what it does is it will assign number three to the total counter. Once this line is executed, then Wavegroup knows that it needs to wait for three calls of Wavegroup got done. It will wait for three calls of WG dot done. If it only receives two calls of WG dot done, it will not let the execution workflow pass through the WG dot wait function. All right, so before that we have a for loop where we create these workers. So for I in range num workers which is three we call go worker. We create a go routine using the worker function. And we pass it an integer I so that it becomes the worker ID and number of workers. And num workers is three. So the ID will start from zero up to two and we pass it the memory address of sync dot wait group the instance of the wait group struct. So here we pass the actual memory address where the instance of wait group struct exists. So w g is the variable. And if we type ampersand w g then we will get the memory address. And that is what the pointer holds. So the worker needed a pointer to sync dot wait group. And the pointer would have carried the memory address. So we passed the memory address using the ampersand sign before the variable name. And in the next line we call the wait function on the wait group instance. And this wait function will wait for all the go routines to finish and the counter to become zero. All right. So counter will become zero as w dot done gets executed and it keeps on decrementing the weight group counter by one. All right. So if two weight group turns have been executed two goroutines have finished, then it will still wait for the third one because the total value that the weight group holds is three w dot add was passed on number three, and that's why it needs to decrement three times before it reaches zero. So let me write here blocking mechanism. So this will block the thread. And once it unblocks then only we will see the printout of all workers finished. Now we can experiment with this. So copy this and Then we do this. So we add one to this. And so we need to again understand the execution flow and how goroutines are treated and how fast our execution is. All right. So we know what's happening here. We reach here. Now in this loop the go execution the go runtime sees that it has a go keyword. It extracts this out again in the next loop. It extracts the second instance out third instance again it is extracted out. Now this is happening very very very fast. All right. Even before the first go routine has started, its first line, even before the first go routine has executed its first line, all the three go routines have been extracted out from the main thread and the main thread comes to the next line, it executes wait group dot. Wait. It's not like this line was skipped. It executes it. But by this time also those go routines did not reach the point of adding one to the wait group. And that's why there was nothing to wait and the execution workflow moved to the next line and we see the printout. All workers finished onto our terminal. So as I stated earlier, go is very, very fast. Go execution is extremely fast. Go runtime is really efficient and that's why even before those go routines start the execution, the go runtime is so fast that it reaches the end of the main function. And that's why we never add these counts inside the go routines. All right, this is Wrong practice. And here let's bring that back. And I mentioned that this is the correct way of adding counter to weight groups. Uh, let me write the full form. Weight groups. Okay. So you have now seen a mistake that could be made if we are not experimenting, if we do not understand the concept or if the concept is not known to us. So now, you know, uh, if this question might have come up in your mind. Now you know why this is not done like this, right? Why are we adding to weight groups? Why are we adding the counter to the weight groups in the main function, or in another function, but not inside a go routine. Perfect. So this was a little small caveat in the weight groups. So now let's move on to our next example. This example, the very first example of weight groups was intentionally a small example, a small code. Because I did not want to include channels, I wanted to first prove that to you that you don't have to use channels to wait for go routines. All right. Because go has offered us another mechanism to work with go routines, wait for them, coordinate with them, and perform a blocking mechanism and get the desired output. All right. So that's why I did not include channels at all in this first example, to keep it as simple as possible, and also to intentionally avoid channels and prove to you the utility of weight groups. However, in our next example, now this time we are going to use channels because in advanced cases and quite frequently in our in our daily work, we will combine weight groups with channels. So we are going to take the same example that we did in our last lecture. And we are going to send some results back and get the results printed out onto the terminal. So for that we will use channels. So let me mention a heading here. Basic example with out using channels. Perfect. And comment this out. So. Great. Now an example with channels perfect. So the first thing that we always do is create a worker. So funk worker it's going to take an ID of int type. And this time we are using channel. So results is a channel. And this channel is receiving values inside it into it. So the arrow is going into the channel and the values are of type int. So let me remove this space and int. Then the third argument is going to be weird group because we are using weird groups. Type is a pointer type. So pointer sync dot wait group perfect. And now we define the function. So the first thing that we do when we are using wait group inside a go routine is to defer the execution of weight group dot. Done. Next, we print a confirmation that we are starting with the worker. So worker percentage T. So worker ID and the worker is starting. So that's it. And a new line character. Don't forget that ID. And now we simulate some work by using time dot sleep time dot second. You can use more seconds like two seconds to observe the difference. But I'm going to use just one second. Otherwise the time duration of our lecture increases because we we just have to wait for the output for two seconds three seconds. So that's why I'm using just one second mostly. But you can increase the time to see the difference in the output to observe how they are finishing when they are finishing. So a longer time duration actually helps us understand and observe a few things that we don't observe in a short time duration. So it's okay to use a longer time duration in your own examples on your own computers. All right, so time.sleep time dot second, simulate some work. And next. Now we pass some value to results. So results receives values and it's going to be ID but multiplied by two. And that's it. So now we have some values that we are being that we are passing to the results channel. Finally, now that we have performed our task, we confirm that we are finished. So Dot print l n no print f worker id percentage d finished and percentage d sorry, not percentage d its id. All right, now let's move on to our main function. And first thing is the weight group initialize the weight group sink dot weight group. That's it. And now we first create the number of workers. And we fixed that to three. And now number of jobs. Let's use three initially. And then in the second run we will increase the number of jobs as well. All right. So in the first one let's keep it simple and let's scroll this above. Okay. Perfect. So now what do we do? The first thing that we do that we ensure is about weight groups. All right. So weight group dot add exactly. So weight group dot add. And now let's launch the workers. So for I in range num workers. We start the go routine. So go worker. And the first argument is id int. So pass I to it. Then the results channel. So we don't have a results channel yet. Let's create the results channel. Make channel of type int and num jobs. So it will be a buffered channel with three buffers. And now we pass it the results channel. And then finally we pass the wait group. But not like this. But we want the memory address so that the source gets updated directly from within the goroutine. So for that we pass the memory address which is ampersand w g. All right. And now because results is a channel we need to close it once it is done. All right. So we start another go routine go func. We declared inside the main function because it's a very small function. Execute this immediately. An anonymous function and w g dot wait we wait inside the go routine. So now this example is a different implementation of WG weight. And here our execution will not be blocked and we don't want it to be blocked. We want to receive the values in real time, right? We don't want to receive the values once all workers, once all goroutines have finished, we want to receive values in real time. And that's why we need to extract WG dot wait outside the main thread so that we can keep printing the results as they are generated. Okay. All right, so WG dot wait is in a goroutine and similarly close the channel results. So now because we are waiting for these goroutines to finish close results will only close the results channel once all goroutines have finished and Goroutines will finish once they have all sent values to the results channel. All right. So it will not create any problem for us. Okay. Because even if we do this then it will be a problem if we are not waiting. And if you are just closing the results in a go routine, it might close the results channel before we send values to the results channel, or maybe after. Maybe if this code routine gets executed a little later after these go routines, then also maybe after 1 or 2 values this channel will be closed. So to only close this channel, once all the values have been passed, then we use WG dot wait before close results. So what that will do is that it will close the results channel once all the three worker threads, once all the three workers have Finished. So even if one worker has finished results, channel will still remain open because the results channel is being used by another worker ID and moreover, worker group wait is blocking and it will only let the execution move to the next line. Once all the goroutines have finished all the goroutines that this wait group is waiting for to finish. All right. Perfect. Now we just print out the results. So for result in range results I use Fmt.println and result. End result. Let me correct this. All right let's save this and run this. So. Go! Run! Wait! Group! Go! And let's make it a little bigger. Perfect. And first, let's clear this. So, so clear. And now go! Run! And what happened? Okay. This happened. Ah. Okay. So worker 012 started. Worker 120 starting. So we don't know how Goroutines are assigned threads behind the scenes. So a different order and the finishing is also different. So worker two finished and then worker one and then worker zero. So 210. And here it's 021. And the results are four, two and zero and zero for two. So yeah, we have different order in different runs of our program. So here we have used channel. And here we are implementing a non-blocking mechanism because we want to receive the values real time. Okay. So that's why we used a go routine to get this weird group dot wait method outside of the main thread and let the execution flow move to the next line of our main function. All right, so it's exactly the same what we were doing earlier. It's just that we have included the results channel. So this time what we are doing is we are passing the results channel. And instead of sending values to the results channel from inside the main function, we are sending the value to the results channel from inside worker. All right. So worker is using the id value. And then multiplying it by two and then sending that value to the results channel. And next as we have already discussed the weight group dot weight method is extracted out of the main thread. And if we want to create this example, just like what we did in the worker pool, we can create another channel of jobs. Okay. So we create it as tasks. We created tasks variable last time. And so that's what we will do here. And now we will use tasks channel of type receiving. So this is a channel from which we will be receiving int values. And now we range over tasks to pass value to results. So for. For task in range tasks. And we move it up. And instead of ID we use task. That's it. All right. So this is the first step towards making it like the way we used to. In the worker pool. All right. So now let's pass values to the tasks channel. So for I in range. Tasks and sorry not tasks num jobs. And this time we are going to create more jobs. So we. Created ten jobs I think last time or five. But you can create more. So I'm going to. Create five jobs. Let's create a space here. All right. So tasks is going to receive a value of I. And now we move to go worker and pass it. What was the first one. Tasks okay. So we have to use tasks as the second argument. All right. So now that we have our program ready we need to close the tasks channel as well. And we close the channel once we have sent all the channels sorry sent all the values to that channel. So close. And the tasks channel. Perfect. Let's save it. All right. And one more thing that I noticed is that this was wrong earlier. All right? This was wrong earlier. And it still is wrong. So earlier the number of jobs were equal. And that's why it didn't matter. But now the number of jobs are different. And we are adding the number of workers and not the number of jobs. So this is NUM workers and not num jobs. So in the previous run also it should have been NUM workers because we have to always add the number of workers to the weight group. The weight group is for workers only, not for tasks. So ideally, even if we have the same number of workers and jobs, still we have to follow the proper convention that we pass the correct variable to the weight group. And now we also see that our workers have IDs starting from one. So let's add one to I. And our workers will have IDs starting from one. Now. All right. Now let's save this and run this. Go run. Wait. Group perfect. So 123. And then again three one. And we have values okay. Let's change the values as well. So tasks instead of 0 to 4, let's send values 1 to 5. Save it and run it again. Perfect. So we are sending values two, four, six, eight, ten. And our workers are starting and finishing on time. So let's run this again. And here we go. So 123 and then 132. So in the previous run 123 and 123. So the order of finishing is different each time. Perfect. So in this case we converted our previous example from worker pools into a wait group where we are closing both the channels. Now in this function you may increase the number of jobs handled by the limited number of workers and observe the result. All right. Okay. So next let's take another example. And this time we are going to use a struct. So just like our ticketing example I'm going to use a real world scenario. So imagine we have a construction site with multiple workers each responsible for a specific task such as digging, laying bricks or painting. And we want to simulate these tasks being performed concurrently and ensure that all tasks are completed before announcing that the construction is finished. So let's see how we do that using structs. So let's comment this out. And start our new example. So. Construction example. And first thing that we do is we define a struct. So it's going to be a worker struct. So it's going to be a worker struct with id as int and the task as string. Next we perform a task. So perform task will simulate a worker performing a task func. Now let's assign a receiver to this function right. So this function is part of the struct is associated with the struct worker. So we assign a receiver by using a pointer receiver because we will be accessing and modifying the fields of worker struct so w. So we create an instance using a pointer receiver. So w and then a pointer receiver. And this is called perform task. If we are not exporting it we can use lowercase perform task as well. All right. So but we are using capital w in worker. So uppercase perform task is okay here as well. Now w g. And this is going to be again a pointer sync dot wait group. All right. So first thing that we do defer wait group dot done. Always declare the defer statements in the early part of the function okay. Now Fmt.printf And worker ID percentage d, started percentage s and then a new line character. And the first thing is the integer value. The worker id so w.id and the next is w dot task. That's it. And now we simulate that the work was done and it took some time. So for that time dot sleep and time dot second you may use more than one second okay. This is not percentage D. This is S. All right. So you may use more than one second for time dot sleep. And then we complete the task. So. Worker ID. Percentage D. Finished. And newline character with w.id and w dot task. And that's it. Now let's start our main function. Func main. And we define the tasks. So define tasks to be performed by workers. All right. So the tasks is a list a slice of strings. So slice of string. And then it's going to carry values such as digging and then laying bricks finally Painting. All right. And now we create a worker group. So actually let's create the worker group at the top. Sorry. Wait group I sometimes call it worker group. It's just we are creating workers. It's kind of worker group as well. But yes, call it the official term wait groups. So wait group and then sync dot wait group. All right. Now we use a for loop to start workers for each task. And this time we are not using a variable to fix the number of workers because every task will be performed by a separate worker. So we have a list of tasks. And that is what we are going to use to start each worker. So for index Comma task in range tasks. We know that an array, when ranged over, returns index and the value that it contains at that index. So we are storing the index and the value as I and task. All right let me scroll it above. Perfect. So we now start our worker so worker. And now call the worker function. Sorry not the worker function. The worker struct and assign it id id capital. And it's going to be I plus one because I is going to be zero. And then assign it task as task. And in the next line we perform wg dot ad and wg Baghdad is not being done inside a go routine. All right. It is inside a loop, but not a go routine. And this time we are adding the count of these workers one by one. Instead of adding them all together, however, we can add them all together as well. We can just calculate the length of tasks, slice, and then add that in just one go at the top. But I just wanted to demonstrate how you can add the values to the weight group one by one, but they just don't have to be in a go routine in a loop. Yes, because and as we are creating a worker, as we are creating a go routine, we are adding one to the weight group count. So this is also a good mechanism. So now go worker sorry go small lowercase worker. So worker dot perform From task and then pass it the weight group so w g. Okay. So I don't have to use asterisk here okay. Perfect. All right. So this is also a very good practice that we add one to the weight group as we start each worker. All right. So now we wait for all workers to finish by calling w dot weight. And next final confirmation that the program has finished. So construction is finished. Fix the typo and fmt.println. Construction finished. Save this and run this. First clear this and now run! Here we go. Worker three started painting. Worker one started digging, and worker two started laying bricks. And worker two then finished laying bricks. Worker three finished painting and worker one finished digging. As soon as they were all finished, our construction was also finished. So this was again a little close to reality, but a very, very small example of how we use weight groups. And we are not fixing the weight group count based on a number. This time we are using a list of tasks, and similarly you may have a list of tasks or a struct that you need to use to fix the number of workers. And this is how you are going to do it. So let's go over this example. We define a worker struct representing a worker at the construction site. Each worker has an ID and a task. The perform task method simulates a worker performing a task. It prints messages indicating when a worker starts and finishes a task. It also decrements the wait group counter when the task is finished using defer WG dot. Done. Then we come to the main function. In the main function, we create the wait group WG, and we create a slice of tasks for which we are going to create workers. After that, we use a loop to start goroutines for each task and for each goroutine we call the perform task method of a worker instance. Before starting each goroutine we increment the wait group counter using WG dot add one. Finally, we call WG dot. Wait for all workers to finish their tasks once all tasks are completed. WG dot wait will not block the main execution thread and the execution thread will move on to the next line. And we print a message indicating that the construction is finished. So this example demonstrates how to use sync dot wait group to synchronize concurrent goroutines and wait for them to finish before proceeding. Now in our previous lecture, we created a ticketing system. You can try to create that on your own using wait groups. It's going to take very few modifications, but it's going to be a good practice and it's going to work out pretty well once you use weight groups in that example, so try and do that on your own. It won't take a long time, maybe ten minutes or so, or maybe just five minutes. Not not too long because you already have the whole program ready. Now let's come to some best practices associated with weight groups. First, avoid blocking calls inside go routines. Always ensure that go routines do not block or hang indefinitely as it may lead to wait group waiting forever. Always use defer to call done in go routines to ensure that it gets called, even if an error occurs or the go routine exits prematurely. And always ensure proper use of add. Ensure that the number passed to add is accurate because I did make a mistake instead of NUM workers. I used NUM jobs and that could have created a problem if NUM jobs and NUM workers had different values. So always ensure that we pass correct values. Accurate values to the add function. Because if we are misusing add or if we are entering incorrect value to the add method, it can lead to deadlocks or premature exits. And let's actually try to intentionally create an error. So instead of one let me use two here. So if I do this and run this, it will end up with a greater counter value. But our work done will only decrease the counter three times. So let's see what happens. And we have an error. So if our total count of wait group dot done does not meet the total count of weight group dot add, then we will have unnecessary deadlocks. Next best practice is that when dealing with a large number of go routines, make sure to manage resources and avoid overloading the system. So by following these best practices, we avoid some common pitfalls like a mismatch between Add and done. So, by following the best practices, we will ensure that the number of add calls matches the number of done calls. Because mismatched calls can lead to unexpected blocking or premature completion, if incorrectly, you added less number of workers to the total count and you created perfect number of W.G. dot done in the go routines. But the declaration of WG Baghdad had less total number. So in that case, the weight group will finish prematurely and there will be some go routines which will be left in a limbo. So if we have a greater number of total counter in Baghdad, then the actual number of go routines, hence the actual number of WG done calls, then in that case we will have a deadlock. But if we have less number of total counter when we added the total counter to WG dot add and we have more number of go routines, hence we have more count of WG dot done, then in that case there will be many go routines which will be left in a limbo and WG dot wait will not wait for those additional go routines to finish and it will let the program execution move forward. So be careful about that. Overall, weight groups in go are essential for managing and synchronizing concurrent go routines, ensuring that multiple operations complete efficiently and reliably. Go's sync Dot wait Group package provides a powerful and straightforward way to handle this synchronization. In this lecture, we went through a comprehensive overview of wait groups in Go, covering fundamental concepts, usage patterns, various scenarios, and best practices. By understanding and applying these techniques, you can write robust, efficient and maintainable concurrent code. We have discussed best practices in detail, provided practical examples, and highlighted common pitfalls to avoid. Ensuring you can confidently implement wait groups in your go applications.
*/
