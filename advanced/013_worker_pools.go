// 013 Worker Pools

package main

import (
	"fmt"
	"time"
)

// Step: 2)
type ticketRequest struct {
	personID   int
	numTickets int
	cost       int
}

// simulate processing of ticket requests
func ticketProcessor(requests <-chan ticketRequest, results chan<- int) {
	for req := range requests {
		fmt.Printf("Processing %d ticket(s) of personID %d with total cost %d\n", req.numTickets, req.personID, req.cost)
		// simulate processing time
		time.Sleep(time.Second)
		results <- req.personID
	}

}

func main() {
	numRequests := 5
	price := 5
	ticketRequests := make(chan ticketRequest, numRequests)
	ticketResults := make(chan int)

	// start ticket processor/worker
	for range 3 {
		go ticketProcessor(ticketRequests, ticketResults)
	}

	// send ticket requests
	for i := range numRequests {
		ticketRequests <- ticketRequest{personID: i + 1, numTickets: (i + 1) * 2, cost: (i + 1) * price}
	}
	close(ticketRequests)

	for range numRequests {
		fmt.Printf("Ticket for personID %d processed successfully!\n", <-ticketResults)
	}
}

// Step: 1)
// ============= BASIC WORKER POOL PATTERN
// func worker(id int, tasks <-chan int, results chan<- int) {
// 	for task := range tasks {
// 		fmt.Printf("Worker %d processing task %d\n", id, task)
// 		// Simulate work
// 		time.Sleep(time.Second)
// 		results <- task * 2
// 	}
// }

// func main() {
// 	numWorkers := 3
// 	numJobs := 10
// 	tasks := make(chan int, numJobs)
// 	results := make(chan int, numJobs)

// 	// Create workers
// 	for i := range numWorkers {
// 		go worker(i, tasks, results)
// 	}

// 	// Send values to the tasks channel
// 	for i := range numJobs {
// 		tasks <- i
// 	}

// 	close(tasks)

// 	// Collect the results
// 	for range numJobs {
// 		result := <-results
// 		fmt.Println("Result:", result)
// 	}
// }

/*
A worker pool is a design pattern used to manage a group of workers. And when I say group of workers, I mean go routines. We don't have actual workers, so go routines are working like a group of workers, multiple go routines. And these go routines which are working like group of workers, process a queue of tasks. This pattern helps in controlling the number of concurrent go routines and efficiently handling tasks. The reason why we use worker pools is for resource management. It limits the number of concurrent go routines to avoid overwhelming system resources, and we can limit the number of concurrent goroutines according to our will. It doesn't depend on the number of threads number of cores on the machine. It depends on us How many number of concurrent go routines we want to run, we can limit the number. Even much lesser than the number of cores or threads available. Next reason is task distribution because using worker pools, we efficiently distribute tasks among a fixed number of workers. And obviously the first two points result in the last point, which is scalability. We can now scale the processing of tasks without creating an excessive number of goroutines. Building a basic worker pool requires using the building blocks of basic worker pool, and the building blocks are tasks, the units of work that need to be processed. The next building block is workers. Workers are goroutines that perform the tasks, the jobs. So tasks jobs. It's the same. Next we have a task queue or the job queue, and that is the channel or data structure to hold tasks waiting to be processed. All right. This is the channel or maybe a data structure depending on what you want to use depending on different scenarios. Now how do we implement a worker pool. We create a task channel. This channel holds tasks that workers will process. Next we create worker goroutines. These workers listen on the task channel and process tasks as they arrive. And next we distribute those tasks. We send tasks to the task channel and they will get processed by the workers. And finally, we implement graceful shutdown. We ensure that all workers complete their tasks and exit properly. Now let's see worker pools in action. In an example. First thing that we do is we create a worker function. So this is the function for which multiple instances will be created. And hence multiple workers will be created. Based on this function we are going to call it appropriately worker. And the first parameter is the job ID or the task id. It's going to be an integer. And then the channel from which we will receive the tasks. So this is also int channel. And then finally another channel to which we will send the processed Task. All right. We will receive some values into this function. And then we will simulate some work and then finally send the processed result to another channel. All right. So when we are sending to another channel that means we need a send channel here. So this so we'll name this results because we are sending processed results on this channel. And this is a send only channel. All right. That means that we will be sending data into this channel. And that data will be of int type. Perfect. Now we start a loop and we range over the tasks that need to be processed. So for each task in the range of. We first. Print the worker ID and the job ID or the task number. So instead of print ln I'll use print F and then we'll use some format verbs. So worker percentage d processing task percentage d and a new line character. Now first we need the worker ID and then we need the task perfect. After printing this statement we are going to simulate some work. So we are going to wait for one second. And then we are going to send the result. So that one second is simulating that. We've done some working, that we have performed some complex operation and then we have returned a value. So simulate some work using time dot sleep and then we use time dot second. And next we send the processed results to the results channel. So what we are doing is we are multiplying the task value by two and then sending back to results channel. Perfect. Now let's come to our main function. Here we first fix the number of workers. So number of workers are going to be three. And then we fix the number of jobs. So number of jobs is going to be ten. And now we create a channel tasks channel. and this channel is of type int and has a buffer of ten. So instead of ten. Actually num jobs right. We are not hardcoding the values because that's why we have a variable. And these number of tasks. The buffer of this channel will depend on the number of jobs that we have. Similarly results channel will also. Take int values. And again the buffer will be of num jobs because the same number of results will be generated as the number of tasks. Now let's create workers. So create workers. Let me save this. So what happened. Okay I did not use equal to sign. So yeah. All right. So now it's aligned perfectly. Let's continue now. So create workers we will use for range in this. So for I range NUM workers and then we call workers. And we create goroutine for the worker function. And the first thing that we need to pass as an argument is the worker id. So worker id is I. And then we need to send it the tasks channel. So tasks and then results. And that's it. We have now simply created workers. Now we need to send values to the tasks channel for the workers to process those tasks. Hence now we create a mechanism to send values to tasks channel send tasks to. channel. To the tasks. Send values to the tasks channel. Much better now for I in range num jobs. We send values to tasks and the value is I. Let me scroll it up and now the tasks channel is receiving values from 1 to 10. Now we know that once we have sent all the values to a channel, then we close that channel. So let's close tasks. And now let's collect the. Results for I in range. And we know the number of jobs. And that's the number of results that we will get. So. And now we receive the results into a. And now we receive the results into our result variable. So results from a channel. So to receive we use arrow operator before the channel name. So results. And now we fmt.println and then print the result. And here we actually don't need the I value. So let's remove this. And for range num jobs that's it. All right. So let me scroll to the top and let's understand this function. This function worker represents a worker that receives jobs from channel tasks okay. It receives tasks or jobs. That's a common term for the tasks that we receive that a worker in a worker pool receives. So it receives tasks on the tasks channel, processes them and sends the results back through another channel, which is results. It takes three parameters the id and integer representing the id of the worker. We are going to assign this id parameter to the worker and call it a worker id. Next, we have tasks or receive only channel of integers from which the worker receives tasks. Next we have results another channel, but this time this is a send only channel of integers through which the worker sends back the processed results. Inside the function, there is a loop that continuously receives tasks from the tasks channel until it is closed. For each task received, it prints a message indicating that it started processing the task. Waits for one second simulating work with time dot sleep time dot second, and then sends back the result, which is two multiplied by the value received from the tasks channel. Now when we come to the main function, we see that it starts by defining number of workers. So we fix the number of workers in this variable, and in the next variable we fix the number of jobs, the number of tasks. So I am using tasks and jobs interchangeably. In the parameter I used tasks. And in this variable I used num jobs so that you are familiar with both terms because they are used interchangeably and they mean the same. Next, we create two channels tasks to send tasks to workers and results to receive results from workers. Both are buffered channels with a capacity of NUM jobs. Next we start a loop a loop that ranges over the number of workers, which is fixed and it is three. Hence, when we loop over NUM workers, we end up creating three workers. Inside this loop we are calling the worker function concurrently using the go keyword. Each worker is passed its id, which is denoted by I, and the tasks channel to receive tasks from and the results channel to send results to. After this, we create another loop. This time we range over number of jobs. Number of jobs are ten. Hence, this loop will iterate over ten times until it reaches the next line of execution. So in this loop we pass ten values to the tasks channel, and these values range from 0 to 9, and these values are passed one by one after every iteration. After sending all tasks, the tasks channel is closed using close tasks to indicate that no more jobs will be sent. And finally we use for range to loop over the results channel and this receives all the results from the results channel. And in the next line we print each result separately in each iteration. Now let's run this program and see it in action. So go run worker pools and here we go. Because these are concurrent operations being handled by the go runtime. And the go runtime is using the go runtime scheduler to find available threads. And there's a lot of back and forth that happens here. We are not sure about the sequence of the output. So let's see the output this time. Now in the first run we received values 01243. In the next one we received zero, one, two three. And the result four zero. And then 4026 Six, ten eight. But here it's eight, six, ten. So the order is a little jumbled, but not too much because yes, the earliest go routine will always return before the next few go routines. So it can be a little off but not too off. So this is a design pattern for handling more number of tasks with a small number of workers. Now next let's create another example. And I won't explain that example like I did for this. But the next example is going to be incorporating a real world scenario. So let's incorporate a ticket selling and buying mechanism using worker pools. So let me comment everything here. Basic worker pool pattern. The first thing that I do is I hit caps lock to print in lowercase, and then after that, what I do is create a struct ticket request struct and it's going to have three fields a person id and num tickets, the number of tickets that this person is buying and the cost of ticket. So int int and int. Now let's simulate processing of ticket requests from the Buyers. So the buyers are going to request processing of tickets from the workers. So when we are simulating processing of ticket requests, we are creating workers. So we are going to write the function for the ticket processor who are going to receive requests from ticket buyers. So requests channel will receive the requests. So channel and the type is ticket request. And then we have a results channel. And that is going to be of type int perfect. And now we start looping over the ticket requests. So for request in range requests we use Fmt.printf and we type processing percentage D tickets. So the number of tickets and it could be just one ticket as well. So now this is better tickets of person. Just to keep our example simple, we are using integers for everything. So we'll give a person the person ID of 12345 like that. So person id and then percentage d width total cost percentage d. So the cost of tickets is going to vary depending on the number of tickets that the buyer is purchasing. Now we use req dot number of tickets. And then for next placeholder req dot person ID and then req dot cost. Wonderful. And now we simulate processing time. So simulate processing time of tickets. So now we put it to sleep for just one second. So time dot second. And next we send the processed ticket IDs to results channel. So results is going to receive. So results channel is receiving recorded person ID. Perfect. That's it. And now we create the main function. And first we fix the number of requests to five. You can fix more number of requests. I'm just trying to keep it simple so that the terminal will not be flooded with too many requests, and we can see the output in a legible way. So the number of requests five. And then I'm going to fix the price of each ticket also to five. Next we create the channels which will send and receive data. So so ticket requests is going to be a channel of type ticket request. So ticket request and it's going to have a buffer equal to the number of requests. Similarly we have ticket results. Another another channel of type int. Let's scroll a little perfect. Let's scroll a little perfect. So now let's start the ticket processor or the worker. And we are going to create again three workers to work on the job queue. So for range three. And now we create the three workers using go routines. So go to get processor. And it's going to take ticket requests as the first argument and ticket results as the second argument. And now we send ticket requests. So now we are creating a job queue for these processors. So these ticket processors are going to have a queue of people who are requesting to purchase ticket from them. So for I in range num requests we are going to send values to ticket requests channel and the value will be of type ticket request. So the first field is person ID we give it an ID of I and then the next field is number of tickets. So we use I multiplied by two. But one thing that we need to remember is the range starts from zero. So it's going to be 024 and zero multiplied by two is zero zero multiplied by anything is zero. And the person ID will also be zero. So this is going to create a zero person ID, zero number of tickets and zero cost. So let's add one to the number. So I plus one and close in parentheses. And then finally we need to include cost. Cost will be I plus one multiplied by price. And that's it. So now we close this channel. Once all the requests have been sent to the ticket requests channel. So ticket requests and this will close it. And now simply range over the results. So for range num requests. And here we are going to print a confirmation that for ticket for person ID has been processed successfully. So it's going to be a string ticket for person ID percentage D processed successfully. And it's going to be print f I'm going to correct that. Let me first receive value here. So we are going to receive ticket results and correct this to print F. All right. So this looks good. Let's see if it works. Go run worker pools. And here we go. All right. The output is not good because we are not introducing new line character in printf. Let's run this again. Great. So now this is the same mechanism that we followed earlier. But we are associating it with a kind of a practical scenario. And if you notice the output then you will notice that the workers are processing The requests in batch. So we have three workers. That means we will be able to handle three requests together. So I'm going to run this again and notice the output. So three together. And then as those workers get free and then they process additional requests. And at the same time we also need to print the results of the requests that have been processed. So that also gets processed in between. Now let's go over some best practices associated with worker pools. The first point is that we need to limit the number of workers. Avoid creating too many workers. Use a reasonable number based on available system resources. Do not stress out your system because otherwise the efficiency will decrease and when working with worker pools, Properly start, stop and clean up workers to prevent resource leaks and ensure graceful shutdowns. And in order to do that, also implement timeouts. Use timeouts to avoid indefinite blocking or hanging operations, and worker pools can get complex in real world scenarios, so monitor the performance of the worker pool and log relevant information to diagnose issues or optimize performance. Overall, worker pools in Go are a powerful tool for managing concurrent task processing, improving efficiency, and optimizing resource utilization by understanding the key concepts, best practices, and advanced patterns of worker pools, you can effectively implement and manage them in your go applications to handle large volumes of tasks concurrently.
*/
