// 003 Hello World!

package main

import "fmt"

func main() {
	fmt.Println("Hello World!")
}

// go build 03_hello.go

/*
All right, everyone. So now we are diving into first look at programming with a simple program in go. Don't worry if you're new to programming. This session is designed to introduce you gently to the world of coding. So we are going to first start with the name of our file. So let's call it hello Dot go. So whenever we are writing code in go language we have to make files with dot go extension. So it's just like when we are working in JavaScript we make dot js files. When we were working in go we make dot go file. So let's create the first program. I'm going to first type everything out. And then we'll go over the program step by step. So in go the first thing that we do is we declare the package. So the entry point of any go program and a bigger program or a smaller program or just one file, it has to be package main. So the first thing that I do is declare the package under the name of main, and then we use the import statement and we import fmt package. We are importing fmt package to use the println command. So now in order for me to run this program, I can click on this play button at the top right corner of your screen. Or as soon as you hover on this icon, it gives you the keyboard shortcut Ctrl alt N. You can also do that. Or in VSCode, what you can do is you can hit control with the apostrophe or the tilde key, the key that is before number one. So use that key along with control. And then you will open the terminal here. So instead of using this the code runner I would recommend you to run your code by using the commands that I am going to share with you because that's what you will be doing mainly, and you should be knowing the commands that are essential when programming with go. So this is a shortcut, but this is used only when you have a lot of experience. And when you know the command that you will not forget the commands that are required to run a go program. But now but right now you are at the beginning stage. So it's better to use the commands manually so that you get into a habit so that those commands are grasped by your brain and you don't forget them in the long run. So the command that we use to run any go program is go run. And then the file name. The file name here is hello Dot go. So let me break down this program. The program starts with package declaration. Every go program starts with a package declaration. A package is a way to organize your code into reusable units. The main package is special, however, because the main package tells go that this is the starting point of your program, this is the entry point of the bigger program, so it can be. So there can be hundreds of files in that project. There could be thousands of lines in each file, but there will be a package main which will have a function main. And that main function will be the entry point of the program. Next we come to the import statement. The import keyword is used to include external packages that provide functionality beyond the basic go language. Features. Here we are importing the FMT package, which stands for format and it's used for formatted input and output. Next we come to main function. The func main is the entry point of every go program. Every time you're writing a program, you have to write the main function. That's how you will be able to run the Go program. When you run a go program, it looks for the main function. If it's not able to find the main function, it won't be able to run. So let me let me show you how. So function man or let me change it to function route. All right. We don't have a function main here. Now let's see what happens. See. So it's the inherent nature. It is by design that that go language compiler will look for the function main in order for any go program to run successfully. Otherwise it won't be able to run that program. So we always declare the entry point of our program with func main. We declare a main function and that's how we are able to run our program. And next inside the main function we use the print statement. Fmt.println is a function provided by the fmt package. It's used to print or output text to the console. In this case, hello world is a string a sequence of characters enclosed in double quotes. Strings in programming are used to represent text. Go inherently supports Unicode, enabling it to effectively handle text from diverse languages across the globe. So that's another strength of go language. So we don't just have to use English here, but we can use any language in the Unicode system. Now in this program, the most important part initially is for you to understand the importance of the package name, main and the main function. The package name main holds special significance. It informs the go compiler that this package is intended to be transformed into a stand alone executable. Every executable program you create in go! Must include a main package. When the compiler encounters the main package, it expects to. Find a function named main within it. This function serves as the entry point of the program defining where execution begins. Without a main function, the compiler cannot generate a binary executable. So now as you see, if I change the name of the package, I keep the function main, but I change the name of the package from main to root. It is still not able to run the program because in order for go to compile and execute our program, it needs the package main and the function main. So we have to keep that in mind whenever we are programming in go. Subsequently, when developing software for professional applications. Be it an API, a command line tool, or any other package intended for real world use, efficiency in deployment becomes crucial. Rather than repeatedly compiling our program, we compile it once and store the compiled output for future execution. This process is achieved using the go build command. So let me show you what it's all about. Let me clear this and we run the go build command with. Hello Dot. Go. Let me first change the package name to main so that it will work now. All right. So when I do go build. Hello. What happens is we now have another file which is an executable file. This is not a text file. This is not a code file. This is a binary executable of the code that we have written here. So that's what go does. In go we can make our API and then we can make an executable, a binary executable of our API of our program and make it run anywhere we want. So instead of taking our code somewhere, we can just make a binary executable and then use that whenever we want to demonstrate our program to someone else. So now in order for us to run this executable file, all we do is run this file simply like this. And this is what happens. We did not run the command. Go, run! Hello, Dot. Go. We ran the executable file and it resulted in the same output. So this is the strength of the go language. We can make binary executables from our code with such ease.
*/
